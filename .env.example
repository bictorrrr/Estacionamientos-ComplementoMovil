# Example environment variables for local/dev and Railway
# Use real values only in your local .env or in Railway Variables.

DATABASE_URL=mysql+pymysql://USER:PASSWORD@HOST:PORT/DB_NAME
SESSION_SECRET_KEY=replace_with_a_random_secret_of_32_or_more_chars
SESSION_HTTPS_ONLY=false
